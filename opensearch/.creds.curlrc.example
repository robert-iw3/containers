# Copy to .creds.curlrc and set the real admin credentials. Used by helper
# scripts for authenticated curl calls. .creds.curlrc is gitignored.
user = "admin:changeme-Str0ng!"
cacert = /usr/share/opensearch/config/root-ca.pem
