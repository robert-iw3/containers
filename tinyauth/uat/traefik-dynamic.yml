# Traefik file provider: use the shared UAT CA cert as the default
# certificate for all TLS routers.
tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/tls/tls.crt
        keyFile: /etc/tls/tls.key
