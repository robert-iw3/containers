apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ ansible_default_ipv4.address | default('127.0.0.1') }}:{{ prometheus_port }}
    isDefault: true
