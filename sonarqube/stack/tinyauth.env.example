# tinyauth ↔ Keycloak OIDC client credentials. Copy to tinyauth.env.
# GENERIC_CLIENT_SECRET must match the "tinyauth" client in the imported
# Keycloak realm (keycloak/realm-sonar.json). scripts/render.sh keeps them
# in sync.
GENERIC_CLIENT_ID=tinyauth
GENERIC_CLIENT_SECRET=CHANGE_ME
