# {{ ansible_managed }}
# Bridge for the control server and the tailnet router. headscale takes a fixed
# address (hs_ip) so the router can reach it without container-name DNS.
[Network]
NetworkName=tailscale-net
Subnet={{ net_subnet }}
