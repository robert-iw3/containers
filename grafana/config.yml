deployment_type: docker
grafana:
  image: docker.io/grafana/grafana:12.4.6
  port: 3000
  ldap_config: /etc/grafana/ldap.toml
  plugins:
    - alexanderzobnin-zabbix-app
    - grafana-loki-datasource
loki:
  image: grafana/loki:3.5.7
  port: 3100
  config_file: /etc/loki/loki-config.yml
prometheus:
  image: prom/prometheus:v3.13.1
  port: 9090
  config_file: /etc/prometheus/prometheus.yml
postgres:
  image: docker.io/postgres:17.10-alpine
  port: 5432
  database: grafana
wallarm:
  image: docker.io/wallarm/node:latest
  api_host: ${WALLARM_API_HOST}
  api_token: ${WALLARM_API_TOKEN}
security:
  enable_tls: true
  cert_resolver: letsencrypt
  traefik:
    dashboard: true
    log_level: WARN