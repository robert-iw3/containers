# Single-host deployment target. The Neo4j GraphRAG stack co-locates Neo4j,
# Ollama and GraphRAG for data/GPU locality and runs as one podman-compose
# project. Replace the address with your host.
[neo4j_rag]
neo4j-rag-1 ansible_host=10.0.0.20 ansible_user=deploy
