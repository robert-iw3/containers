# FreeIPA deployment configuration.
# Replace both passwords before deploying; deploy_freeipa.py refuses
# CHANGE_ME values. Generate with: openssl rand -base64 24
domain: example.test
realm: EXAMPLE.TEST
admin_password: CHANGE_ME
dm_password: CHANGE_ME
data_dir: /var/lib/ipa-data
deploy_webui: true
