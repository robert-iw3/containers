---
# Deployment overrides for the kafka_node role (see roles/kafka_node/defaults).
kafka_image: "docker.io/apache/kafka:4.3.1"
container_runtime: podman
kafka_replication_factor: 3
kafka_min_insync_replicas: 2
