# Per-host bridge with DNS so units on the same VM resolve each other by
# container name (cross-host traffic uses the FQDNs from hosts.env).
# Installed on every role host by deploy.sh.
[Network]
NetworkName=portal
