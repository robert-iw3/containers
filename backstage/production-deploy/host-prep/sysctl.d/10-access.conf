# Boundary host: every end-user session is a proxied TCP flow.

# conntrack table sized for many concurrent brokered sessions
net.netfilter.nf_conntrack_max = 262144

# recycle client-side TIME_WAIT sockets for outbound worker connections
net.ipv4.tcp_tw_reuse = 1

net.core.somaxconn = 8192
