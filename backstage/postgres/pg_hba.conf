# TLS-only client access: local socket for maintenance (container exec),
# everything over TCP must be hostssl + scram. Plain TCP gets no match and
# is rejected.
local   all all                trust
hostssl all all 0.0.0.0/0      scram-sha-256
hostssl all all ::/0           scram-sha-256
