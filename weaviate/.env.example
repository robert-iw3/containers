# Copy to .env (or run ./scripts/bootstrap-env.sh to generate random values).
# Consumed at container start; nothing is baked into the image.

# API key for the "admin" identity (full access).
WEAVIATE_ADMIN_KEY=change-me

# API key for the "readonly" identity (query only).
WEAVIATE_READONLY_KEY=change-me
