---
# Deployment overrides for the hadoop_node role (see roles/hadoop_node/defaults).
container_runtime: podman
hadoop_replication: 3
hadoop_cluster_name: "arcanaeum-datalake"
# Registry to pull the hadoop-*:latest images from (push them there after `make build`),
# or leave "" and preload the images on each host.
hadoop_registry: ""
