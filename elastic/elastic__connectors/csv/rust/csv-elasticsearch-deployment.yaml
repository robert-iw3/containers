apiVersion: apps/v1
kind: Deployment
metadata:
  name: csv-elasticsearch
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csv-elasticsearch
  template:
    metadata:
      labels:
        app: csv-elasticsearch
    spec:
      containers:
      - name: csv-elasticsearch
        image: csv-connector:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: CSV_LOG_DIR
          value: "/var/log/csv_data"
        - name: SCHEMAS_FILE
          value: "/app/schemas.yaml"
        - name: ES_HOST
          value: "http://elasticsearch:9200"
        - name: ES_INDEX
          value: "csv-logs"
        - name: BATCH_SIZE
          value: "100"
        - name: BUFFER_TIMEOUT
          value: "30"
        - name: WORKER_COUNT
          value: "4"
        - name: ENABLE_SPLUNK
          value: "false"
        - name: CSV_DELIMITER
          value: ","
        volumeMounts:
        - name: csv-logs
          mountPath: /var/log/csv_data
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
          requests:
            cpu: "0.5"
            memory: "512Mi"
        livenessProbe:
          exec:
            command: ["pidof", "csv-connector"]
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          exec:
            command: ["pidof", "csv-connector"]
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: csv-logs
        persistentVolumeClaim:
          claimName: csv-logs