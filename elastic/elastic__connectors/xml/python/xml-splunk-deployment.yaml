apiVersion: apps/v1
kind: Deployment
metadata:
  name: xml-splunk
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xml-splunk
  template:
    metadata:
      labels:
        app: xml-splunk
    spec:
      containers:
      - name: xml-splunk
        image: xml-connector:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: XML_LOG_DIR
          value: "/var/log/xml_data"
        - name: SCHEMAS_FILE
          value: "/app/schemas.yaml"
        - name: SPLUNK_HEC_URL
          value: "https://splunk:8088/services/collector"
        - name: SPLUNK_TOKEN
          value: "your-splunk-hec-token"
        - name: BATCH_SIZE
          value: "100"
        - name: BUFFER_TIMEOUT
          value: "30"
        - name: WORKER_COUNT
          value: "4"
        - name: ENABLE_SPLUNK
          value: "true"
        volumeMounts:
        - name: xml-logs
          mountPath: /var/log/xml_data
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
          requests:
            cpu: "0.5"
            memory: "512Mi"
        livenessProbe:
          exec:
            command: ["pidof", "python3"]
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          exec:
            command: ["pidof", "python3"]
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: xml-logs
        persistentVolumeClaim:
          claimName: xml-logs