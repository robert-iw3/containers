apiVersion: apps/v1
kind: Deployment
metadata:
  name: xml-elasticsearch
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xml-elasticsearch
  template:
    metadata:
      labels:
        app: xml-elasticsearch
    spec:
      containers:
      - name: xml-elasticsearch
        image: xml-connector:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: XML_LOG_DIR
          value: "/var/log/xml_data"
        - name: SCHEMAS_FILE
          value: "/app/schemas.yaml"
        - name: ES_ENABLED
          value: "true"
        - name: ES_HOST
          value: "http://elasticsearch:9200"
        - name: ES_INDEX
          value: "xml-logs"
        - name: BATCH_SIZE
          value: "100"
        - name: BUFFER_TIMEOUT
          value: "30"
        - name: WORKER_COUNT
          value: "4"
        - name: CONFIG_FILE
          value: "/app/deploy_config.yaml"
        volumeMounts:
        - name: xml-logs
          mountPath: /var/log/xml_data
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
          requests:
            cpu: "0.5"
            memory: "512Mi"
        livenessProbe:
          exec:
            command: ["pidof", "xml-connector"]
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          exec:
            command: ["pidof", "xml-connector"]
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: xml-logs
        persistentVolumeClaim:
          claimName: xml-logs